{
  "name": "pairrank",
  "displayName": "PairRank",
  "version": "0.1.0-beta",
  "description": "Pairwise Ranking",
  "author": {
    "name": "Hauke D",
    "email": "haukex@zero-g.net"
  },
  "license": "GPL-3.0-or-later",
  "type": "module",
  "source": "src/index.html",
  "browserslist": "defaults",
  "_comment": "NOTE naming the test script `test` apparently always causes the VSCode test runner to use that",
  "scripts": {
    "clean": "git clean -dxf -e node_modules",
    "lint": "tsc --noEmit && eslint",
    "build": "npm run clean && npm run lint && parcel build",
    "all-test": "npm run build && playwright test",
    "start": "npm run clean && parcel --port 1234"
  },
  "devDependencies": {
    "@eslint/js": "^9.38.0",
    "@parcel/transformer-sass": "^2.16.0",
    "@parcel/transformer-typescript-tsc": "^2.16.0",
    "@playwright/test": "^1.56.1",
    "@stylistic/eslint-plugin": "^5.5.0",
    "@types/node": "^24.9.1",
    "@types/webpack-env": "^1.18.8",
    "eslint": "^9.38.0",
    "parcel": "^2.16.0",
    "serve": "^14.2.5",
    "typescript": "^5.9.3",
    "typescript-eslint": "^8.46.2"
  },
  "dependencies": {
    "merge-insertion": "^1.0.1",
    "simpledotcss": "^2.3.7"
  }
}
